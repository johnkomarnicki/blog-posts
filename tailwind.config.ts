import prose from "@tailwindcss/typography";

/** @type {import('tailwindcss').Config} */
export default {
  plugins: [prose],
  theme: {
    container: {
      center: true,
      padding: "2rem",
    },
  },
};
